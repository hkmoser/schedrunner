# boot-notify

Post-boot health check via Pushcut.

## Status

Scaffolded with the schedrunner `new-repo` skill. See `CLAUDE.md` for how to put
this repo on a schedule or enable auto-deploy via schedrunner.
