google-cloud-bigquery>=3.25,<4
# Secrets backend for the in-app Settings page — stores app-entered config/secrets in
# Google Secret Manager via the Mac's Application Default Credentials.
google-cloud-secret-manager>=2.20,<3
